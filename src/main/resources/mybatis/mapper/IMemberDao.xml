<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.study.member.IMemberDao">


    <select id="getTotalRowCount" resultType="int">

    </select>


    <select id="getMemberList" resultType="com.study.member.MemberVO">


    </select>


    <select id="getMember" resultType="com.study.member.MemberVO" parameterType="String">

    </select>


    <update id="updateMember">
        UPDATE member
        SET
             id             =#{memId}
            ,name            =#{memName}
            ,password        =#{memPass}
            ,address         =#{memAdd}
            ,phnum           =#{memHp}
            ,email           =#{memMail}
            ,birth           =#{memBir}


        WHERE mem_id = #{memId}

    </update>


    <delete id="deleteMember" parameterType="String">
        DELETE FROM member
        WHERE memId = :currentUserId;
--      currentUserId를 현재 로그인한 사용자의 아이디를 나타내는 변수 또는 파라미터로 대체
    </delete>


    <insert id="insertMember">
        INSERT INTO member (
            id,
            name,
            password,
            address,
            phnum,
            email,
            birth
        ) VALUES (
                    #{memId}
                    #{memName}
                    #{memPass}
                    #{memAdd}
                    #{memHp}
                    #{memMail}
                    #{memBir}
                 );
    </insert>


</mapper>